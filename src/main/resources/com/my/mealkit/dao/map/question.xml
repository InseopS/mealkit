<?xml version='1.0' encoding='utf-8' ?>
<!DOCTYPE mapper PUBLIC '-//mybatis.org//DTD Mapper 3.0//EN'
	'http://mybatis.org/dtd/mybatis-3-mapper.dtd'>
<mapper namespace='com.my.mealkit.dao.map.QuestionMap'>
	<select id='selectQuestions' resultType='question'>
		select * 
		from questions
		order by question_num
	</select>
	
	<select id='selectAdminQuestions' resultType='question'>
		select * 
		from questions
	</select>
	
	<select id='selectQuestion' resultType='question'>
		select *
		from questions
		where question_num = #{questionNum}
	</select>
	
	<select id='selectAdminQuestion' resultType='question'>
		select *
		from questions
		where question_num = #{questionNum}
	</select>
	
	<insert id='insertQuestion'>
		insert into questions(question_num, question_title, question_content, question_regdate)
		values(question_num_seq.nextval, #{questionTitle}, #{questionContent}, sysdate)
	</insert>
	
	<insert id='insertAdminQuestion'>
		insert into questions(question_num, question_title, answer_content, question_regdate)
		values(#{questionNum}, #{questionTitle}, #{answerContent}, sysDate)
	</insert>
	
	<update id='updateQuestion'>
		update questions
		set question_title = #{questionTitle}
			question_content = #{questionContent}
			question_regdate = #{questionRegdate}
		where question_num = #{questionNum}
	</update>
	
	<update id='updateAdminQuestion'>
		update questions
		set question_title = #{questionTitle}
			answer_content = #{answerContent}
			question_regdate = #{questionRegdate}
	</update>
	
	<delete id='deleteQuestion'>
		delete questions
		where question_num = #{questionNum}
	</delete>
	
	<delete id='deleteAdminQuestion'>
		delete questions
		where question_num = #{questionNum}
	</delete>
</mapper>